{{>partials/header}}
<section class="detail">
    <h1>Solicitudes de eliminación pendientes</h1>
</section>
<section class="stack">
    {{#solicitudes}}
    <article class="card">
        <h2>{{titulo}}</h2>
        <p><strong>Motivo:</strong> {{motivo}}</p>
        <div class="actions">
            <form method="post" action="/admin/solicitudes/{{id}}/aprobar">
                <button type="submit" class="primary">Aprobar</button>
            </form>
            <form method="post" action="/admin/solicitudes/{{id}}/rechazar">
                <button type="submit" class="danger">Rechazar</button>
            </form>
        </div>
    </article>
    {{/solicitudes}}
    {{^solicitudes}}
    <p>No hay solicitudes pendientes.</p>
    {{/solicitudes}}
</section>
{{>partials/footer}}
